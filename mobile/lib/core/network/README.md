# core/network

Empty on purpose as of Day 1. There is no consumer yet — no REST backend is planned.
This folder gets its first content in Day 2, alongside the course repository (e.g.
connectivity/`NetworkInfo` handling around loading/error states).
